# Secrets — MUST stay first, before anything else lands on disk
.env
.env.local
*.pem
*.key
secrets/

# Live captures from `nellis record` / `nellis doctor`. These are real page
# dumps from your machine — reviewed and shared deliberately, never committed
# by accident.
fixtures/live/

# Data
*.db
*.sqlite
*.sqlite3
data/
cache/
exports/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage

# Playwright
.playwright/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
